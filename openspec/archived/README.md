# Archived OpenSpec Batches

Completed batches move here once they are no longer the active execution plan.

Current archived batches:

- `fork-lift-wave-1` — implemented on `main` in the 2026-06-09 Wave 1-4 commit series

Active in-flight work remains under `openspec/changes/`.
