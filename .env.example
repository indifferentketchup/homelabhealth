# Copy to `.env` and adjust per machine. Compose loads this file automatically.

# Reachable hostname or IP for the host machine. Used as the default base for
# Docker image build args (VITE_PUBLIC_*) and API CORS appendage.
BOOLAB_PUBLIC_HOST=localhost

# Optional: host-published ports
# BOOLAB_PORT_API=9300
# BOOLAB_PORT_808NOTES=9304

# `npm run dev` / Vite dev: proxy /api → this URL (defaults to http://127.0.0.1:9300)
# BOOLAB_VITE_API_PROXY=http://localhost:9300

# Split UI only: FastAPI host:port (no http://). Overrides default derived from BOOLAB_PUBLIC_HOST.
# BOOLAB_API_UPSTREAM=localhost:9300

# Database (asyncpg accepts postgresql:// or postgresql+asyncpg://)
DATABASE_URL=postgresql+asyncpg://boolab:boolab@hlh_db:5432/boolab

# Inference: OpenAI-compatible chat completions endpoint (llama-swap, Ollama, etc.).
INFERENCE_URL=http://localhost:8080
# Default model id (must match what your inference endpoint serves).
DEFAULT_MODEL=

# Embeddings: OpenAI-compatible /embeddings endpoint (infinity-emb, etc.).
EMBEDDING_URL=http://localhost:7997
EMBEDDING_MODEL=BAAI/bge-m3

# Reranker: optional. Uses flashrank (CPU) as a fallback when unset or unreachable.
RERANKER_URL=http://localhost:7998
RERANKER_MODEL=BAAI/bge-reranker-v2-m3

# Web search via SearXNG. Leave empty to disable web search.
SEARXNG_URL=
# Optional: host path to searxng settings.yml so Settings → Search can sync engine toggles.
# SEARXNG_SETTINGS_YML=/etc/searxng/settings.yml

# CORS — comma-separated extra origins. The API also auto-appends http://<BOOLAB_PUBLIC_HOST>:9304.
FRONTEND_ORIGIN=http://localhost:5173,http://localhost:9304

# Optional: override SPA build URL (normally derived from BOOLAB_PUBLIC_HOST).
# VITE_PUBLIC_808NOTES_URL=http://localhost:9304
